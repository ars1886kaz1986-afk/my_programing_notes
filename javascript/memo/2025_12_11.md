## Content-Type と　X-CSRF-Token について

### Content-Type
- 送るデータの形式をサーバに教える（JSON など）。FastAPI の body validation に必須
- Content-Type: application/json (FastAPI は bodyをjsonとして読み取る)

### X-CSRF-Token
- CSRF対策のために、Cookie に入っているトークンと一致しているかどうか確認するヘッダー
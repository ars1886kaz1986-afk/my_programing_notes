## fetchについて


### CSRFトークンを取得するときの注意点
```ts
fetch("/api/csrf-token", {
  credentials: "include",
})

credentials: "include" を付けないと Cookie が送信されない

Cookie が無い = サーバ側が CSRF Token 返してくれない

結果、POST すると 403 になる
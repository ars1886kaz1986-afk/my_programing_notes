## alembic 主要なコマンド

**更新**
- 新しい migration ファイルを作る。「今の DB」と「今の SQLAlchemy Models」を比較する
```
docker compose exec api alembic revision --autogenerate -m "create follows table"
```


- DB に変更を適用
```
docker compose exec api alembic upgrade head

```